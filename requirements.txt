# Core application dependencies — these are what Render (or any host) needs
# installed to run the app in production.
anthropic>=1.1.0  # needs client.messages.parse() structured-output support
pydantic>=2.7
typer>=0.12
jinja2>=3.1
sqlalchemy>=2.0
alembic>=1.13  # schema migrations, run automatically by db.py on startup
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9  # FastAPI's multipart/form-data (file upload) parsing
pypdf>=4.0  # resume PDF text extraction
python-docx>=1.1  # resume DOCX text extraction
httpx>=0.27  # outbound webhook delivery (webhooks.py) at runtime
psycopg2-binary>=2.9  # Postgres driver, used when DATABASE_URL is set (db.py)
google-auth>=2.28  # verifies Google Identity Services sign-in tokens (auth.py)
boto3>=1.34  # S3-compatible object storage for resume files (file_storage.py)

# Dev / test only — not required to run the app, only to run `pytest`.
pytest>=8.0
pytest-cov>=5.0
moto[s3]>=5.0  # mocks S3 for file_storage.py tests, no real cloud credentials needed
